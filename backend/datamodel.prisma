# type User {
#   id: ID! @id
#   username: String!
#   concepts: [Concept!]!
# }

type Course {
  id: ID! @id
  name: String!
  concepts: [Concept!]!
#   instances: [Instance!]!
}

# type Instance {
#   id: ID! @id
#   start_date: DateTime!
#   end_date: DateTime!
#   periods: [Int!]! @scalarList(strategy:RELATION)
#   course: Course!
#   concepts: [Concept!]!
# }

type Concept {
  id: ID! @id
  name: String!
  description: String
  # user: User! @relation(link: INLINE)
  official: Boolean! @default(value: false)
  linksFromConcept: [Link!]! @relation(name: "From")
  linksToConcept: [Link!]! @relation(name: "To")
  courses: [Course!]!
}

type Link {
  id: ID! @id
  from: Concept! @relation(link: INLINE, name: "From")
  to: Concept! @relation(link: INLINE, name: "To")
  # user: User! @relation(link: INLINE)
  official: Boolean! @default(value: false)
}